# NEVER commit secrets or downloaded binaries
config.json
venv/
bin/
bin-old/
vertest/
*.mmdb
__pycache__/
*.pyc
.env

# Local runtime state (status snapshot, blocklist, logs) — must survive updates
state/

# edge worker keys (see edge/deploy.sh)
.edge-*-key

edge/.wrangler/
edge/node_modules/
