# NEVER commit secrets or downloaded binaries
config.json
venv/
bin/
bin-old/
vertest/
*.mmdb
__pycache__/
*.pyc
.env

# Local runtime state (status snapshot, blocklist, logs) — must survive updates
state/
